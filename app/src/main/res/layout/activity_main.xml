<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:padding="16dp">

    <Button
        android:id="@+id/show_layout_bounds_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Layout Bounds"
        app:layout_constraintTop_toTopOf="parent"/>

    <Button
        android:id="@+id/dont_keep_activities_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Dont Keep Activities"
        app:layout_constraintTop_toBottomOf="@id/show_layout_bounds_button"/>

    <Button
        android:id="@+id/leak_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="LeakCanary Example"
        app:layout_constraintTop_toBottomOf="@id/dont_keep_activities_button"/>

</androidx.constraintlayout.widget.ConstraintLayout>